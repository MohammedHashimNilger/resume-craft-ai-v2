# In local dev, leave this unset — Vite's dev server proxies /api to
# http://localhost:5000 (see vite.config.js), so no env var is needed.
#
# In production (Vercel), set this to your deployed Render backend's
# API base URL, e.g.:
VITE_API_URL=https://resume-craft-ai-server.onrender.com/api
